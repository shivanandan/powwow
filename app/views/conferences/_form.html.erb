<%= form_for(@conference) do |f| %>
  <% if @conference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@conference.errors.count, "error") %> prohibited this conference from being saved:</h2>

      <ul>
      <% @conference.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :workshop_registration_flag %><br>
    <%= f.check_box :workshop_registration_flag %>
  </div>
  <div class="field">
    <%= f.label :workshop_close_flag %><br>
    <%= f.check_box :workshop_close_flag %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
